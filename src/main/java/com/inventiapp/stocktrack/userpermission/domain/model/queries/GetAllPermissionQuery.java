package com.inventiapp.stocktrack.userpermission.domain.model.queries;

/**
 * Record que representa la consulta para obtener todos los permisos.
 * (Puede estar vacío si no se necesitan filtros).
 */
public record GetAllPermissionQuery() {
}