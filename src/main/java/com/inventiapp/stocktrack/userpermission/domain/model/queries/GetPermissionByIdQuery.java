package com.inventiapp.stocktrack.userpermission.domain.model.queries;

/**
 * Record que representa la consulta para obtener un permiso por su ID.
 */
public record GetPermissionByIdQuery(Long id) {
}